version: "2"
sql:
  - name: users's repository
    engine: mysql
    schema: db/schema.sql
    queries: internal/app/user/repository/query.sql
    gen:
      go:
        package: repository
        out: internal/app/user/repository
        emit_interface: true